# winhlp32
Hacked up version of WINE's winhlp32 program so it can build in VS2005.

The included EXE was built in a Windows 2000 virtual machine running Visual Studio 2005. Use the `m.cmd` script to build; it just invokes rc and cl directly on the files.
